name: company_analytics
description: "Internal Flutter analytics facade for Facebook App Events and Singular."
version: 0.0.1

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  facebook_app_events: ^0.21.0
  singular_flutter_sdk: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  yaml: ^3.1.3

flutter:
